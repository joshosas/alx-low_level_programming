#include "main.h"

/**
 * _islower - function checks for lower case letters
 * @c: character to check
 * Return: 0 or 1
 */
/* _islower: function checks lower case for alphabets */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
